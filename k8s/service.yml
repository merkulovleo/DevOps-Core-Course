apiVersion: v1
kind: Service
metadata:
  name: devops-info
  labels:
    app: devops-info
spec:
  type: NodePort
  selector:
    app: devops-info
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
